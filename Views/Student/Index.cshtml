@model IEnumerable<student>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>
    
    <div class="container">
        <table class="table table-bordered ">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Name</th>
                    <th scope="col">Age</th>
                    <th scope="col">more</th>
                </tr>
            </thead>
            <tbody class="thead-light">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.id</td>
                        <td>@item.name</td>
                        <td>@item.age</td>
                        <td>
                            <a class="btn btn-primary mr-2" href="/student/details/@item.id" role="button">more</a>
                            <a class="btn btn-primary mr-2" href="/student/updateview/@item.id" role="button">update</a>
                            <a class="btn btn-danger mr-2" href="/student/warning/@item.id" role="button">delete</a>
                        </td>
                    </tr>
                }

            </tbody>

        </table>
        <br />

        <a class="btn btn-primary" href="/student/createview" role="button">Add a student</a>


    </div>
</body>
</html>
